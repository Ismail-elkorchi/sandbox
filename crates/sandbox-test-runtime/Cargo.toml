[package]
name = "sandbox-test-runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
sandbox-protocol = { path = "../sandbox-protocol" }

[features]
test-backend = []
